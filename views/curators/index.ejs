<!-- Boiler plate -->
<%- include("../partials/header") %>
<!-- Nav Bar -->
<%- include("../partials/nav") %>
<!-- Header -->
<h1>Users home page</h1>
<!-- body -->
      <h2>List of Galleries</h2>
      <% if(!curators.length) { %>
        <p>There are no Galleries available yet.</p>
      <% } %>
<div class="curatorIndex">
        <% curators.forEach(curator => { %>

<div class="ui list">
  <div class="item">
    <i class="users icon"></i>
    <div class="content">
      <a href="/curators/<%= curator._id %>"> <%= curator.user %> </a>
    </div>
  </div>
</div>        
</li>    
        <% }) %>
</div>

<!-- Boiler plate -->
<%- include("../partials/footer") %>